dist: xenial
language: python
python:
    - "3.5"
    - "3.6"
    - "3.7-dev"

# command to install dependencies
install:
    - pip install .[gpl]
    - pip install nltk

script: pytest

notifications:
    email: false
